import { Route } from '@angular/router';
import { FormBuilderComponent } from './form-builder.component';

export const formBuilderRoutes: Route[] = [
    {
        path: '',
        component: FormBuilderComponent,
    }
];
